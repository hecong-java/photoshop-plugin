---
phase: 01-configuration-system
plan: 04
type: execute
wave: 4
depends_on:
  - 01-00
  - 01-03
files_modified: []
autonomous: false
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - INTG-01
user_setup: []

must_haves:
  truths:
    - "User can verify config file is read correctly"
    - "User can verify only configured nodes appear in UI"
    - "User can verify unconfigured nodes don't break workflow"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete configuration system works end-to-end in the actual plugin environment.

Purpose: Confirm all components integrate correctly and meet user requirements.
Output: Verified working configuration system.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-configuration-system/01-03-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of configuration system</name>
  <files>N/A - checkpoint task</files>
  <action>
    Human verification checkpoint for the complete configuration system.

    This task pauses execution for user to verify:
    1. Build passes without errors
    2. Config file loads correctly in UXP environment
    3. Only configured nodes display in plugin UI
    4. Workflow generation succeeds with filtered display
    5. Config changes reflected after plugin reload
  </action>
  <what-built>
    Complete configuration system including:
    - Bridge handler for reading config file (fs.readPluginConfig)
    - Config file template (node-config.json)
    - Config service with validation (config.ts)
    - Config store with helper functions (configStore.ts)
    - UI filtering in Draw.tsx
  </what-built>
  <how-to-verify>
    ## Verification Steps

    ### 1. Verify Build Passes
    ```bash
    cd code/webapp
    npm run build
    ```
    Expected: Build completes without errors

    ### 2. Verify Config File Exists
    - Check that `PS-plugin/ningleai/node-config.json` exists
    - Open the file and verify it contains valid JSON with nodes array

    ### 3. Test in Photoshop (UXP Runtime)
    1. Start Photoshop with the plugin loaded
    2. Open the plugin panel
    3. Connect to ComfyUI server
    4. Select a workflow that contains KSampler, CLIPTextEncode, etc.

    ### 4. Verify Config Loading
    - Open browser DevTools console (if available in UXP)
    - Look for log message: "[Config] Loaded config with N node entries"
    - Or check that filtering is applied (fewer inputs shown than without config)

    ### 5. Verify Node Filtering
    - Confirm KSampler node shows only: seed, steps, cfg, sampler_name, scheduler, denoise
    - Confirm CLIPTextEncode shows only: text
    - Confirm nodes NOT in config (like Note, Reroute) don't appear
    - Confirm VAEDecode and SaveImage appear (in config with no inputs filter = show all)

    ### 6. Verify Workflow Submission
    - Click Generate to submit the workflow
    - Confirm generation succeeds (unconfigured nodes use defaults)
    - Check ComfyUI console for any errors

    ### 7. Test Config Changes
    - Edit node-config.json to add/remove a node
    - Reload the plugin
    - Verify UI reflects the changes
  </how-to-verify>
  <verify>
    <automated>N/A - human verification checkpoint</automated>
  </verify>
  <resume-signal>Type "approved" if verification passes, or describe any issues found</resume-signal>
  <done>
    - User confirms build passes
    - User confirms config loads correctly
    - User confirms only configured nodes appear
    - User confirms workflow submission works
    - User approves or describes issues
  </done>
</task>

</tasks>

<verification>
Human verification required - this checkpoint validates:
1. Build succeeds
2. Config file is read correctly
3. Only configured nodes appear in UI
4. Workflow submission still works
5. Config changes take effect on reload
</verification>

<success_criteria>
- Build passes without errors
- Config file loads correctly in UXP environment
- Only configured nodes display in plugin UI
- Only specified inputs show for each node
- Workflow generation succeeds with filtered display
- Config changes reflected after plugin reload
</success_criteria>

<output>
After completion, create `.planning/phases/01-configuration-system/01-04-SUMMARY.md`
</output>
