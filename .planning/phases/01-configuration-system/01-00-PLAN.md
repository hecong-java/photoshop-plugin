---
phase: 01-configuration-system
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - code/webapp/src/services/config.test.ts
  - code/webapp/src/stores/configStore.test.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Test files exist and are discoverable by Vitest"
    - "Test scaffolds have placeholder tests that run and fail"
    - "Test infrastructure is ready for Wave 1 implementation"
  artifacts:
    - path: "code/webapp/src/services/config.test.ts"
      provides: "Test scaffold for config service"
      exports: ["describe", "it", "expect"]
    - path: "code/webapp/src/stores/configStore.test.ts"
      provides: "Test scaffold for config store"
      exports: ["describe", "it", "expect"]
  key_links: []
---

<objective>
Create test file scaffolds for the configuration system to enable TDD workflow in subsequent waves.

Purpose: Establish test infrastructure before implementation so Wave 1+ tasks can use actual Vitest verify commands.
Output: Two test files with placeholder tests that fail, ready for TDD implementation.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Vitest test pattern from existing project -->

From vite.config.ts (test framework config):
```typescript
// Vitest is configured with defaults
// Test files should be named *.test.ts and placed alongside source files
```

Standard Vitest test structure:
```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';

describe('ModuleName', () => {
  beforeEach(() => {
    // setup
  });

  it('should do something', () => {
    expect(true).toBe(true);
  });
});
```

Mock pattern for Bridge:
```typescript
vi.mock('../services/upload', () => ({
  sendBridgeMessage: vi.fn(),
  hasBridgeTransport: vi.fn(),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.test.ts scaffold</name>
  <files>code/webapp/src/services/config.test.ts</files>
  <action>
    Create `code/webapp/src/services/config.test.ts` with placeholder test scaffolds for the config service.

    The file should include:

    1. **Imports:**
    ```typescript
    import { describe, it, expect, beforeEach, vi } from 'vitest';
    import { loadPluginConfig, validateConfig, DEFAULT_CONFIG } from './config';
    ```

    2. **Mock setup:**
    ```typescript
    vi.mock('./upload', () => ({
      sendBridgeMessage: vi.fn(),
      hasBridgeTransport: vi.fn(),
    }));
    ```

    3. **Placeholder test suites** (these will fail until implementation exists):
    ```typescript
    describe('loadPluginConfig', () => {
      it('should return DEFAULT_CONFIG when no bridge transport', async () => {
        // Placeholder - will be implemented in Wave 1
        expect.assertions(1);
        expect(true).toBe(true); // Remove when real test is written
      });

      it('should return parsed config when file exists', async () => {
        // Placeholder - will be implemented in Wave 1
        expect.assertions(1);
        expect(true).toBe(true); // Remove when real test is written
      });

      it('should return DEFAULT_CONFIG on parse error', async () => {
        // Placeholder - will be implemented in Wave 1
        expect.assertions(1);
        expect(true).toBe(true); // Remove when real test is written
      });
    });

    describe('validateConfig', () => {
      it('should filter invalid node entries', () => {
        // Placeholder - will be implemented in Wave 1
        expect.assertions(1);
        expect(true).toBe(true); // Remove when real test is written
      });

      it('should preserve valid class_type and inputs arrays', () => {
        // Placeholder - will be implemented in Wave 1
        expect.assertions(1);
        expect(true).toBe(true); // Remove when real test is written
      });
    });

    describe('DEFAULT_CONFIG', () => {
      it('should have empty nodes array', () => {
        // Placeholder - will be implemented in Wave 1
        expect.assertions(1);
        expect(true).toBe(true); // Remove when real test is written
      });
    });
    ```

    The placeholder tests pass trivially now but will be replaced with real assertions in Wave 1 when the implementation is added.
  </action>
  <verify>
    <automated>test -f code/webapp/src/services/config.test.ts && npx vitest run config.test.ts --passWithNoTests && echo "Test scaffold created"</automated>
  </verify>
  <done>
    - config.test.ts exists in services folder
    - File contains describe blocks for loadPluginConfig, validateConfig, DEFAULT_CONFIG
    - Tests run without syntax errors
    - Vitest discovers the test file
  </done>
</task>

<task type="auto">
  <name>Task 2: Create configStore.test.ts scaffold</name>
  <files>code/webapp/src/stores/configStore.test.ts</files>
  <action>
    Create `code/webapp/src/stores/configStore.test.ts` with placeholder test scaffolds for the Zustand config store.

    The file should include:

    1. **Imports:**
    ```typescript
    import { describe, it, expect, beforeEach, vi } from 'vitest';
    import { useConfigStore } from './configStore';
    ```

    2. **Mock setup:**
    ```typescript
    vi.mock('../services/config', () => ({
      loadPluginConfig: vi.fn(),
      DEFAULT_CONFIG: { version: '1.0', nodes: [] },
    }));
    ```

    3. **Placeholder test suites** (these will fail until implementation exists):
    ```typescript
    describe('useConfigStore', () => {
      beforeEach(() => {
        // Reset store state between tests
        vi.clearAllMocks();
      });

      describe('shouldDisplayNode', () => {
        it('should return true if node in config or config empty', () => {
          // Placeholder - will be implemented in Wave 2
          expect.assertions(1);
          expect(true).toBe(true); // Remove when real test is written
        });

        it('should return false if config has nodes but classType not found', () => {
          // Placeholder - will be implemented in Wave 2
          expect.assertions(1);
          expect(true).toBe(true); // Remove when real test is written
        });
      });

      describe('getAllowedInputs', () => {
        it('should return null if node not in config (show all)', () => {
          // Placeholder - will be implemented in Wave 2
          expect.assertions(1);
          expect(true).toBe(true); // Remove when real test is written
        });

        it('should return inputs array if node has inputs specified', () => {
          // Placeholder - will be implemented in Wave 2
          expect.assertions(1);
          expect(true).toBe(true); // Remove when real test is written
        });

        it('should return null if node in config but no inputs (show all)', () => {
          // Placeholder - will be implemented in Wave 2
          expect.assertions(1);
          expect(true).toBe(true); // Remove when real test is written
        });
      });

      describe('loadConfig', () => {
        it('should populate store from Bridge', async () => {
          // Placeholder - will be implemented in Wave 2
          expect.assertions(1);
          expect(true).toBe(true); // Remove when real test is written
        });
      });
    });
    ```

    The placeholder tests pass trivially now but will be replaced with real assertions in Wave 2 when the store implementation is added.
  </action>
  <verify>
    <automated>test -f code/webapp/src/stores/configStore.test.ts && npx vitest run configStore.test.ts --passWithNoTests && echo "Test scaffold created"</automated>
  </verify>
  <done>
    - configStore.test.ts exists in stores folder
    - File contains describe blocks for shouldDisplayNode, getAllowedInputs, loadConfig
    - Tests run without syntax errors
    - Vitest discovers the test file
  </done>
</task>

</tasks>

<verification>
After completing tasks:
1. Verify config.test.ts exists and runs: `npx vitest run config.test.ts`
2. Verify configStore.test.ts exists and runs: `npx vitest run configStore.test.ts`
3. Both test files are discoverable by Vitest
</verification>

<success_criteria>
- config.test.ts scaffold exists with placeholder tests
- configStore.test.ts scaffold exists with placeholder tests
- Both test files run without errors (pass with placeholder assertions)
- Test infrastructure ready for Wave 1 TDD implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-configuration-system/01-00-SUMMARY.md`
</output>
