---
phase: 05.1-plugin-performance-fix
plan: 02
subsystem: performance
tags: [base64, async, executeAsModal, UXP, photoshop-plugin]

# Dependency graph
requires:
  - phase: 05.1-plugin-performance-fix
    provides: "keyboard shortcut passthrough from 05.1-01 (no code dependency, sequential planning only)"
provides:
  - "async arrayBufferToBase64 helper yielding to event loop between chunks"
  - "reduced executeAsModal scope moving file I/O and base64 conversion outside modal state"
  - "_exportedFile pattern for passing file references out of modal scope"
affects: [05.1-plugin-performance-fix, PS-plugin, main.js]

# Tech tracking
tech-stack:
  added: []
  patterns: [async-chunked-base64, modal-scope-minimization, _exportedFile-reference-pattern]

key-files:
  created: []
  modified:
    - PS-plugin/ningleai/main.js

key-decisions:
  - "32KB chunk size preserved from original sync implementation for async helper"
  - "Yield to event loop via setTimeout(0) between chunks rather than larger batching"
  - "_exportedFile pattern used to pass file reference out of modal scope for deferred reading"
  - "exportActiveLayerPngInternal returns _exportedFile instead of base64; callers handle read+convert"
  - "importImageAsLayer and importBase64AsLayer left unchanged as they are pure batchPlay operations"

patterns-established:
  - "Async base64 conversion: use arrayBufferToBase64(buffer) instead of sync String.fromCharCode.apply loops"
  - "Modal scope minimization: only wrap batchPlay/DOM mutations in executeAsModal, move file I/O outside"
  - "File reference passing: return _exportedFile from modal scope, read+convert in non-modal context"

requirements-completed: [PERF-02, PERF-03]

# Metrics
duration: 3min
completed: 2026-04-17
---

# Phase 05.1 Plan 02: Async Base64 & Modal Scope Reduction Summary

**Async arrayBufferToBase64 helper with event-loop yielding and reduced executeAsModal scope for export operations**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-17T01:10:57Z
- **Completed:** 2026-04-17T01:14:37Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Replaced all 3 synchronous String.fromCharCode.apply base64 conversion sites with a shared async helper that yields to the UXP event loop between 32KB chunks
- Moved file reading and base64 conversion outside executeAsModal scope in exportActiveLayerPng and exportSelectionPng, reducing modal state duration to only Photoshop-mutating operations
- Fixed byteLength reference in comfyui.fetch handler to use arrayBuffer instead of removed bytes variable

## Task Commits

Each task was committed atomically:

1. **Task 1: Create async arrayBufferToBase64 helper and replace all 3 sync conversion sites** - `7ca2f37` (feat)
2. **Task 2: Reduce executeAsModal scope in exportActiveLayerPng and exportSelectionPng** - `f057e1c` (feat)

## Files Created/Modified
- `PS-plugin/ningleai/main.js` - Added async arrayBufferToBase64 helper, replaced 3 sync conversion sites, reduced executeAsModal scope for export functions

## Decisions Made
- Kept 32KB chunk size from original implementation to maintain memory characteristics
- Used setTimeout(0) yield between chunks as the lightest-weight event loop yield mechanism
- _exportedFile pattern passes file references out of modal scope for deferred reading

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Fixed stale `bytes.byteLength` reference in comfyui.fetch handler after removing the `bytes` variable (replaced with `arrayBuffer.byteLength`) - discovered and fixed inline during Task 1.

## Next Phase Readiness
- All sync base64 patterns eliminated from main.js
- executeAsModal scope minimized for export operations
- Ready for 05.1-03 (any remaining performance improvements)

## Self-Check: PASSED

---
*Phase: 05.1-plugin-performance-fix*
*Completed: 2026-04-17*
