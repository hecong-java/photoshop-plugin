# Phase 05.1: plugin-performance-fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-16
**Phase:** 05.1-plugin-performance-fix
**Areas discussed:** Shortcut/focus fix, Thread blocking fix, React render optimization, Scope boundary

---

## Shortcut / Focus Fix

| Option | Description | Selected |
|--------|-------------|----------|
| Keydown passthrough handler | Add keydown listener that detects non-input PS keystrokes and forwards via Bridge | ✓ |
| Auto-blur on idle | Automatically blur webview after idle period so PS regains focus | |
| Minimal focus capture | Only allow focus when interacting with input fields | |
| You decide | Claude picks best approach | |

**User's choice:** Keydown passthrough handler (Recommended)
**Follow-up:** Which shortcuts to forward?

| Option | Description | Selected |
|--------|-------------|----------|
| Known PS shortcuts only | Forward a whitelist of common PS shortcuts | ✓ |
| All non-input keystrokes | Forward any keystroke when not in input field | |
| You decide | Claude picks | |

**User's choice:** Known PS shortcuts only (whitelist approach)
**Notes:** Safer — won't accidentally interfere with webapp input fields

---

## Thread Blocking Fix

| Option | Description | Selected |
|--------|-------------|----------|
| Async chunked conversion | Replace sync loop with async chunked processing yielding to event loop | ✓ |
| Web Worker offloading | Move base64 to Web Worker, fully non-blocking | |
| You decide | Claude picks | |

**User's choice:** Async chunked conversion (Recommended)
**Notes:** Minimal code change, fixes the freeze without UXP Worker complexity

**Follow-up:** executeAsModal scope reduction?

| Option | Description | Selected |
|--------|-------------|----------|
| Reduce modal scope | Only wrap batchPlay in executeAsModal, not entire operation | ✓ |
| Keep current scope | Don't change executeAsModal wrapping | |

**User's choice:** Reduce modal scope
**Notes:** Shortens the blocking window significantly by moving file I/O and base64 outside modal state

---

## React Render Optimization

| Option | Description | Selected |
|--------|-------------|----------|
| Targeted critical fixes | React.memo on list components, useCallback/useMemo for expensive operations | ✓ |
| Full Draw.tsx refactor | Split into smaller components, add memoization everywhere, store selectors | |
| You decide | Claude picks | |

**User's choice:** Targeted critical fixes (Recommended)
**Notes:** Don't refactor Draw.tsx structure — too risky for a performance fix phase

---

## Scope Boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Root causes + easy wins | 3 root causes PLUS console.log cleanup, temp file cleanup, fs.listDownloads opt | ✓ |
| Strictly root causes only | Only the 3 diagnosed issues | |
| Root causes + all secondary | Everything found including duplicate listeners, store selectors | |
| You decide | Claude picks | |

**User's choice:** Root causes + easy wins
**Notes:** Easy secondary fixes (console.log, temp cleanup, file size metadata) included. Larger refactors deferred.

---

## Claude's Discretion

- Exact whitelist of PS shortcuts to forward
- Chunk size for async base64 conversion
- Which console.log statements to remove vs keep
- Temp file cleanup strategy (immediate vs batch)

## Deferred Ideas

- Duplicate message listener consolidation — separate refactor phase
- Zustand store selector optimization — large change surface, not urgent
- Virtual scrolling for history list — only needed for 100+ items
- Draw.tsx structural refactor — high risk, dedicated phase
- Web Worker offloading — async chunking sufficient for now
