---
mode: quick
quick_id: "20260520"
slug: multi-upload-cloud-only
---

# Quick Task: Restrict multi-image upload to cloud model workflows only

## Description
ComfyUI workflow templates (template_type=COMFYUI) in cluster mode don't support multiple images. Currently the multi-image upload UI is shown for all cluster templates, causing errors when used with ComfyUI templates. Restrict multi-image to THIRD_PARTY_API templates only.

## Tasks

### T01: Branch image upload UI by template_type
- **files:** code/webapp/src/pages/Draw.tsx
- **action:** In `case 'image':` block (line ~4892), check `selectedTemplate?.template_type`. For `THIRD_PARTY_API`: keep current multi-image UI. For `COMFYUI`: show single-image UI only (no "添加图片" button, replace existing image on re-upload).
- **verify:** Visual inspection of the conditional rendering
- **done:** ComfyUI templates show single image upload, cloud model templates show multi-image

### T02: Fix upload handler for single-image mode
- **files:** code/webapp/src/pages/Draw.tsx
- **action:** Modify `handleTemplateImageUpload` (line ~2983) or add a wrapper that replaces instead of appends when the template is COMFYUI type.
- **verify:** ComfyUI template image param replaces existing value instead of appending
- **done:** Single image upload for ComfyUI templates works correctly
