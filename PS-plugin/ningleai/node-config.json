{
  "$comment": "ComfyUI Node Display Configuration - Controls which parameters appear in Photoshop plugin UI. Leave 'inputs' array empty or omit to show all inputs for that node. Nodes not listed here use workflow defaults without showing UI. class_type must match exactly (case-sensitive).",
  "version": "1.0",
  "nodes": [
    {
      "class_type": "LoadImage",
      "inputs": ["image"]
    },
    {
      "class_type": "CLIPTextEncode"
    },
    {
      "class_type": "KIE_NanoBanana2_Image"
    },
    {
      "class_type": "KIE_NanoBananaPro_Image"
    },
    {
      "class_type": "KIE_Seedream45_Edit"
    },
    {
      "class_type": "ImagePadForOutpaint"
    },
    {
      "class_type": "RandomNoise"
    }
  ]
}
